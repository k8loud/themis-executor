### add pod
POST localhost:8080/execute
Content-Type: application/json

{
  "collectionName": "kubernetes",
  "actionName": "AddResourceAction",
  "params": {
    "params": {
      "resourceType": "Pod",
      "resourceName": "nginx",
      "namespace": "default"
    }
  }
}

### sending YAML in JSON is a pain, all whitechars need to be escaped
### not bothering with solving this issue since we're getting rid of REST
